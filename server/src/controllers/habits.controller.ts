import { Request, Response } from "express";
import { sendErrorResponse } from "../utils/sendErrorResponse";

export const createHabit = (req: Request, res: Response) => {
  try {
  } catch (error) {
    sendErrorResponse;
    error;
  }
};

export const getAllHabits = (req: Request, res: Response) => {
  try {
  } catch (error) {
    sendErrorResponse;
    error;
  }
};
